# Unity Catalog Server Configuration

server.port=8080
server.env=dev

# SECURITY: Enable auth before exposing to network
# When enabled, admin token is written to /opt/unitycatalog/etc/conf/token.txt
# Clients pass it as: Authorization: Bearer <token>
server.authorization=disable
