# Lakehouse Infrastructure Configuration
# Copy to .env and fill in values (never commit .env)

# PostgreSQL password for Unity Catalog
UC_DB_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD

# Path to Delta Lake storage (mounted in container as /lake)
LAKE_PATH=/path/to/your/lake

# MinIO (S3-compatible object storage)
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD

# MinIO data path - MUST be local disk, NOT NFS-mounted
# For NAS: /volume1/forge-data
# For dev: ./minio-data (default if not set)
# MINIO_DATA_PATH=./minio-data

# Optional: MLflow database (if using MLflow service)
# MLFLOW_DB_NAME=mlflow
